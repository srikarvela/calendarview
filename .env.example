# Google OAuth client (Web application) from https://console.cloud.google.com
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# Any random string: `openssl rand -base64 32`
AUTH_SECRET=

# Only needed for local dev; Vercel sets this automatically.
# AUTH_URL=http://localhost:3000
